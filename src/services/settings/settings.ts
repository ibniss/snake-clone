// TODO: allow users to change settings
export const Settings = {
  grid: {
    dimension: 40,
    tileSize: 16,
    tileOffset: 0,
    color: 'rgba(245, 245, 245, 1)',
  },
  rootSelector: '#canvasParent',
} as const
